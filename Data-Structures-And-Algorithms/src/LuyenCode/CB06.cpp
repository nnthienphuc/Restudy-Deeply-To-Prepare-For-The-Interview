#include<iostream>

int main() {
    int width, length;
    std::cin >> width >> length;
    std::cout << (width + length) * 2 << std::endl;
    std::cout << width * length << std::endl;

    return 0;
}
